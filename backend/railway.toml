[build]
builder = "nixpacks"

[deploy]
startCommand = "pnpm exec playwright install chromium --with-deps && pnpm start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"
PORT = "3001"
WS_PORT = "3002"
